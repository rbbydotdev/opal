import { absPath } from "@/lib/paths2";

export const SpecialDirsPaths = {
  Trash: absPath("/.trash"),
  Storage: absPath("/.storage"),
  Git: absPath("/.git"),
  Thumb: absPath("/.thumb"),
  Build: absPath("/.build"),
};
