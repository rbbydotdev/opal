"use client";
import { Editor } from "@/components/Editor/Editor";

//@ TODO: rename me
export function EditorCacheContainer({
  workspaceId,
  fileId,
}: {
  skipFetch: boolean;
  workspaceId: string;
  fileId: string;
}) {
  return (
  );
}
