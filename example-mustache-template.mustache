<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{#data.title}}{{data.title}}{{/data.title}}{{^data.title}}Mustache Template{{/data.title}}</title>
</head>
<body>
    <h1>Hello {{#data.name}}{{data.name}}{{/data.name}}{{^data.name}}World{{/data.name}}!</h1>
    
    {{#workspace}}
    <p><strong>Workspace:</strong> {{name}} (ID: {{id}})</p>
    {{/workspace}}
    
    <p><strong>Generated:</strong> {{now}}</p>
    
    {{#images}}
    {{#images.0}}
    <h2>Images in workspace ({{images.length}} total):</h2>
    <div class="image-gallery">
    {{#images}}
        <div style="border: 1px solid #ddd; padding: 10px; margin: 10px;">
            <img src="{{url}}" alt="{{name}}" style="max-width: 200px;">
            <p><strong>{{name}}</strong></p>
        </div>
    {{/images}}
    </div>
    {{/images.0}}
    {{/images}}
    
    {{#fileTree}}
    {{#fileTree.0}}
    <h2>Files in workspace:</h2>
    <ul>
    {{#fileTree}}
        <li><strong>{{name}}</strong> ({{type}})</li>
    {{/fileTree}}
    </ul>
    {{/fileTree.0}}
    {{/fileTree}}
    
    <hr>
    <h3>Mustache Template System</h3>
    <p>This template uses Mustache syntax:</p>
    <ul>
        <li><code>{{variable}}</code> - outputs variable</li>
        <li><code>{{#section}}...{{/section}}</code> - conditional/loop sections</li>
        <li><code>{{^section}}...{{/section}}</code> - inverted sections (false/empty)</li>
    </ul>
</body>
</html>